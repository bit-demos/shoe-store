import { ThemeSchema } from '@learn-bit-react/base-ui.themes.base-theme';

export const darkColorTokens: Partial<ThemeSchema> = {
  textColor: '#fff',
  bgColor: '#000',
  boxShadow: 'none',
  borderColor: '#fff'
};
