---
description: 'A Select component'
labels: ['select', 'form']
---

import { Select } from './select';

## Select with an array of options as numbers

```js live
<Select options={[32, 33, 34]} />
```

## Select with an array of options as text

```js live
<Select options={['apple', 'banana', 'orange']} />
```

## Select with an array of options as text

```js live
<>
  <label htmlFor="fruit-select">Choose a fruit:</label>
  <Select
    id="fruit-select"
    options={['--Please choose an option--', 'apple', 'banana', 'orange']}
  />
</>
```
