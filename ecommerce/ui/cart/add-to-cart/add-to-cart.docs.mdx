---
description: 'A button component that adds items to the cart'
labels: ['button', 'cart']
---

import { AddToCart } from './add-to-cart';

Uses the design button from base-ui and onClick uses cart context to add an item to the cart.

### Component usage

```js
<RemoveFromCart product={{"id": "1", "name": "Product 1", "price": "10.00"}} />
    selectedSize={40}
    selectedColor="red"
    selectedQuantity={3}  />
```
