---
description: 'A ShoesCardGrid component.'
labels: ['card', 'grid']
---

import { ShoesCardGrid } from './shoes-card-grid';
import { mockShoes } from '@learn-bit-react/shoe-store.entity.shoes';

## React Component for rendering a grid of shoe products cards

A basic grid that renders card components for the shoe product

### Component usage

```js
<ShoesCardGrid list={mockShoes()} />
```
